#include <stdio.h>
/**
 * main - A function that print the alphabet in lowercase
 *
 * Retuen: Always 0
 */

int main(void)
{
	int a;

	if (a >= 97 && a <= 122)
	{
		putchar('a');
		putchar('\n');
	}
	return (0);
}
